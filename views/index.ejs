<!DOCTYPE html>
<html lang="<%= currentLang %>">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>
    <%= currentLang==='de' ? 'Enten-Fakten API' : 'Duck Facts API' %>
  </title>

  <link rel="stylesheet" href="/dist/css/main.css" />
</head>

<body data-controller="animation notification">
  <!-- Navigation -->
  <nav class="navbar" data-controller="navigation">
    <div class="nav-container">
      <div class="nav-brand">
        <span>DuckFacts</span>
      </div>

      <div class="nav-controls">
        <!-- Language Switcher -->
        <div class="language-switcher" data-controller="language">
          <button class="lang-toggle" 
                  id="languageDropdown" 
                  data-language-target="toggle"
                  data-action="click->language#toggle">
            <%= currentLang==='de' ? '🇩🇪 DE' : '🇺🇸 EN' %>
              <i class="fas fa-chevron-down" data-language-target="chevron"></i>
          </button>
          <div class="lang-dropdown" id="langDropdown" data-language-target="dropdown">
            <a href="/?lang=en" class="lang-option <%= currentLang === 'en' ? 'active' : '' %>">
              🇺🇸 English
            </a>
            <a href="/?lang=de" class="lang-option <%= currentLang === 'de' ? 'active' : '' %>">
              🇩🇪 Deutsch
            </a>
          </div>
        </div>
      </div>
    </div>
  </nav>

  <!-- Hero Section -->
  <section class="hero" id="hero">
    <div class="hero-background">
      <div class="floating-shapes">
        <div class="shape shape-1"></div>
        <div class="shape shape-2"></div>
        <div class="shape shape-3"></div>
        <div class="shape shape-4"></div>
      </div>
    </div>

    <div class="hero-content">
      <div class="hero-text">
        <h1 class="hero-title">
          <%= currentLang==='de' ? 'Enten-Fakten' : 'Duck Facts' %>
            <span class="gradient-text">API</span>
        </h1>
        <p class="hero-subtitle">
          <%= currentLang==='de' ? 'Entdecke faszinierende, lustige und ungewöhnliche Fakten über Enten mit unserer modernen RESTful API.' : 'Discover fascinating, fun, and quirky facts about ducks with our modern RESTful API.' %>
        </p>

        <div class="hero-buttons">
          <a href="#api" 
             class="btn btn-primary" 
             data-navigation-target="link"
             data-action="click->navigation#smoothScroll">
            <i class="fas fa-code"></i>
            <%= currentLang==='de' ? 'API Testen' : 'Try API' %>
          </a>
        </div>
      </div>

      <div class="hero-visual">
        <div class="duck-illustration">
          <img src="/images/ente.svg" alt="Duck" class="duck-image">
        </div>
        <div class="stats">
          <div class="stat">
            <div class="stat-number">170+</div>
            <div class="stat-label">
              <%= currentLang==='de' ? 'Fakten' : 'Facts' %>
            </div>
          </div>
          <div class="stat">
            <div class="stat-number">2</div>
            <div class="stat-label">
              <%= currentLang==='de' ? 'Sprachen' : 'Languages' %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Features Section -->
  <section class="features" id="features">
    <div class="container">
      <div class="section-header">
        <h2>
          <%= currentLang==='de' ? 'API Features' : 'API Features' %>
        </h2>
        <p>
          <%= currentLang==='de' ? 'Entdecke die mächtigen Features unserer Duck Facts API' : 'Discover the powerful features of our Duck Facts API' %>
        </p>
      </div>

      <div class="features-grid">
        <div class="feature-card">
          <div class="feature-icon">
            <i class="fas fa-random"></i>
          </div>
          <h3>
            <%= currentLang==='de' ? 'Zufällige Fakten' : 'Random Facts' %>
          </h3>
          <p>
            <%= currentLang==='de' ? 'Erhalte zufällige Enten-Fakten für endlose Überraschungen' : 'Get random duck facts for endless surprises' %>
          </p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">
            <i class="fas fa-globe"></i>
          </div>
          <h3>
            <%= currentLang==='de' ? 'Mehrsprachig' : 'Multilingual' %>
          </h3>
          <p>
            <%= currentLang==='de' ? 'Verfügbar in Englisch und Deutsch' : 'Available in English and German' %>
          </p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">
            <i class="fas fa-rocket"></i>
          </div>
          <h3>
            <%= currentLang==='de' ? 'Schnell & Zuverlässig' : 'Fast & Reliable' %>
          </h3>
          <p>
            <%= currentLang==='de' ? 'Hochperformante API mit minimaler Latenz' : 'High-performance API with minimal latency' %>
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Facts Showcase -->
  <section class="facts-showcase" id="facts" data-controller="facts">
    <div class="container">
      <div class="section-header">
        <h2>
          <%= currentLang==='de' ? 'Beispiel Fakten' : 'Sample Facts' %>
        </h2>
        <p>
          <%= currentLang==='de' ? 'Hier sind einige interessante Enten-Fakten aus unserer Sammlung' : 'Here are some interesting duck facts from our collection' %>
        </p>
      </div>

      <div class="facts-grid" data-facts-target="grid">
        <% exampleFacts.forEach(function(fact, index) { %>
          <div class="fact-card" data-index="<%= index %>">
            <div class="fact-icon">
              <i class="fas fa-quote-left"></i>
            </div>
            <p class="fact-text">
              <%= fact %>
            </p>
            <div class="fact-number">#<%= index + 1 %>
            </div>
          </div>
          <% }); %>
      </div>

      <div class="facts-actions">
        <button class="btn btn-outline" 
                id="loadMoreFacts" 
                data-facts-target="loadButton"
                data-action="click->facts#loadMore">
          <i class="fas fa-refresh"></i>
          <%= currentLang==='de' ? 'Mehr Fakten laden' : 'Load More Facts' %>
        </button>
      </div>
    </div>
  </section>

  <!-- API Documentation Section -->
  <section class="api-section" id="api" data-controller="api-tester">
    <div class="container">
      <div class="section-header">
        <h2>
          <%= currentLang==='de' ? 'API Dokumentation' : 'API Documentation' %>
        </h2>
        <p>
          <%= currentLang==='de' ? 'Integriere unsere Duck Facts API in deine Anwendung' : 'Integrate our Duck Facts API into your application' %>
        </p>
      </div>
      <div class="api-actions">
        <button class="btn btn-primary" 
                id="tryApiBtn" 
                data-api-tester-target="button"
                data-action="click->api-tester#test">
          <i class="fas fa-play"></i>
          <%= currentLang==='de' ? 'API Jetzt Testen' : 'Try API Now' %>
        </button>
      </div>
    </div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="footer">
    <div class="container">
      <div class="footer-content">
        <div class="footer-brand">
          <span>DuckFacts API</span>
        </div>

        <div class="footer-links">
          <a href="/api-docs">
            <%= currentLang==='de' ? 'Dokumentation' : 'Documentation' %>
          </a>
          <a href="https://github.com/MobilGame06/Duck-Facts">GitHub</a>
        </div>

        <div class="footer-social">
          <p>
            <%= currentLang==='de' ? 'Erstellt mit' : 'Made with' %>
              <i class="fas fa-heart"></i>
              <%= currentLang==='de' ? 'von' : 'by' %>
                <a href="https://info.mobilserver.xyz">MobilGame06</a>
          </p>
        </div>
      </div>
    </div>
  </footer>

  <!-- Scripts -->
  <script src="/dist/js/main.js"></script>
</body>

</html>
